const express = require('express')

const router = express.Router();

//const {addTrain, getTrain} = require("../controllers/trainController")
//router.post('/', addTrain);
//router.get('/', getTrain);



module.exports = router;